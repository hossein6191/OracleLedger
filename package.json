{
  "name": "oracle-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Every on-chain oracle update for a pair, side by side: Chainlink, Chronicle, RedStone. One month of history, refreshed every 30 seconds.",
  "engines": { "node": ">=20" },
  "scripts": { "start": "node server.mjs" },
  "dependencies": { "ethers": "^6.13.0" }
}
